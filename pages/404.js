import React from "react"
import Layout from "../components/layout"

const NotFoundPage = () => (
  <Layout>
    <h1>WRONG TURN</h1>
    <p>You've somehow stumbled onto a page that doesn't exist, hopefully you find your way back home.</p>
  </Layout>
)

export default NotFoundPage
